spring:
  application:
    name: GraalVMDemo
management:
  server:
    port: 8887
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true